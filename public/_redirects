# App routes (admin, size, blog, …) are static Next exports.
# CMS permalinks are handled by functions/_middleware.js (no catch-all here —
# Cloudflare evaluates redirects before static assets, so /:slug broke /admin).
